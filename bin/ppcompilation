#!/usr/bin/env node
var path = require('path');
var fs = require('fs');
var gulp = require('gulp');
var lib = path.join(path.dirname(fs.realpathSync(__filename)), '../lib');
require(lib + '/gulpfile.js').compile();
